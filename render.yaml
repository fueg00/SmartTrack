services:
  # Backend API
  - type: web
    name: smarttrack-api
    env: node
    rootDir: backend
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://getsmarttrack.io
      - key: TURSO_DATABASE_URL
        sync: false
      - key: TURSO_AUTH_TOKEN
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: PRO_PRICE_ID
        sync: false
      - key: ENT_PRICE_ID
        sync: false

  # Frontend Static Site
  - type: static
    name: getsmarttrack-io
    env: static
    rootDir: frontend/dist
    publishDir: .
    routes:
      - type: rewrite
        source: /api/*
        destination: https://smarttrack-api.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html
